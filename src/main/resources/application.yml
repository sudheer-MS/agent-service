server:
  port: 8081

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/

spring:
  application:
    name: AGENT-SERVICE
  config:
    import: configserver:http://localhost:8888
